@model List<Position> 
@{
    int count = 0;
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Position Table</h4>

            <a asp-action="Create" asp-controller="Postion" class="btn btn-success">Create Position</a>

            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                No
                            </th>
                            <th>
                                Name
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Position position in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                                <td>
                                    @position.Name
                                </td>
                                <td>
                                    <a asp-action="Update" asp-controller="Postion" asp-route-id="@position.Id" class="btn btn-primary"><i class="mdi mdi-grease-pencil"></i></a>
                                    <a asp-action="Delete" asp-controller="Postion" asp-route-id="@position.Id" class="btn btn-danger"><i class="mdi mdi-delete"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>